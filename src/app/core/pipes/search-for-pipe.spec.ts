import { SearchForPipe } from './search-for-pipe';

describe('SearchForPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchForPipe();
    expect(pipe).toBeTruthy();
  });
});
